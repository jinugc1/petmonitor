// PLACEHOLDER — replace by running:
//
//   dart pub global activate flutterfire_cli
//   flutterfire configure
//
// which generates the real file from your Firebase project. This stub
// exists only so the project compiles before configuration; it fails fast
// at runtime with a clear message.

import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnsupportedError(
        'Firebase is not configured yet. Run `flutterfire configure` '
        '(see docs/DEPLOYMENT.md) to generate lib/firebase_options.dart.',
      );
}
